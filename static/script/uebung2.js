function setText(element) {
    // let text = document.querySelector("#inp").value; // Nimmt sich den aktuell eingegeben Text und 
    // speichert ihn in die Variable text
    document.querySelector("#output").innerText = element.value; // ERsetzt den aktuellen Text durch den obigen
}
